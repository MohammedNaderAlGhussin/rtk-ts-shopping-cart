import { Typography, Box } from "@mui/material";

const About = () => {
  return (
    <Box ml={10} p={5}>
      <Typography variant="h4" pb={3}>
        About
      </Typography>
    </Box>
  );
};

export default About;
